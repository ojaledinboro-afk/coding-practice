# User Input Storage

A Python project to store and retrieve user input data.

## Features
- Saves user input to a file
- Reads stored data
- Simple command-line interface

## How to Run
```bash
python user_input_storage.py
